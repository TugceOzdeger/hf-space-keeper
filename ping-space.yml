name: Keep Hugging Face Space Awake

on:
  schedule:
    - cron: "*/15 * * * *" 
  workflow_dispatch:  

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: |
          echo "Pinging your HF Space..."
          curl -s https://infinitevalueme-infinite-agent.hf.space > /dev/null
          echo "✅ Space pinged successfully!"
